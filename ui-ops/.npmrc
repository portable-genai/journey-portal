# The Angular/Vite builder declares Less as an optional peer. This shell uses plain CSS, so
# installing that unused preprocessor only adds its vulnerable image parser to the build graph.
# Keep optional native compiler bindings; omit peer packages that are not direct dependencies.
omit=peer
